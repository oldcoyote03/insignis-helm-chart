apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insignis.fullname" . }}-test-connection"
  labels:
    {{- include "insignis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insignis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
