apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insignis.fullname" . }}-test-connection"
  labels:
    {{- include "insignis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: busybox
      command: ['curl']
      args: ['{{ include "insignis.fullname" . }}:{{ .Values.service.port }}/test']
  restartPolicy: Never
